#include<stdio.h>

int main(){
    int a, b, c, sum;
    printf("Enter the value of a b c \n", a, b, c);
    scanf("%d%d%d", &a, &b, &c);
    sum = a + b + c;
    printf(" the value of sum is %d", sum);
    scanf("%d", &sum);

    
    return 0;
}