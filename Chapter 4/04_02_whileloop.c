#include<stdio.h>

int main(){
    int a = 0;
    //only true value will b e calculated in loops//
    scanf("%d", &a);
    while(a>10){
        printf("%d\n", a);
        a++; // i++ = i + 1  and you have to enter the value off a after running the program //
    }
    
    
    return 0;
}